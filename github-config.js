/* GitHub Pages frontend configuration. Edit GAS_WEB_APP_URL after deploying GAS as Web App. */
window.APP_GITHUB_CONFIG = Object.freeze({
  GAS_WEB_APP_URL: "PASTE_GAS_WEB_APP_EXEC_URL_HERE",
  BRIDGE_MODE: "github-bridge",
  BRIDGE_TIMEOUT_MS: 30000,
  REQUEST_TIMEOUT_MS: 90000
});
